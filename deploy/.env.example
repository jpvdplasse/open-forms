# REQUIRED: Set your domain name (must have DNS pointing to this server)
DOMAIN=openforms.cluis.nl

# REQUIRED: Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=CHANGE_ME

# REQUIRED: Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DB_PASSWORD=CHANGE_ME
